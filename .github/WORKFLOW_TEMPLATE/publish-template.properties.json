{
  "name": "C# Package CI",
  "description": "Publishes a C# package project using.",
  "iconName": "dotnet",
  "categories": ["C#", "Publish"],
  "inputs": [
    {
      "name": "project_path",
      "description": "Relative path to the .csproj file",
      "required": true
    },
    {
      "name": "project_name",
      "description": "Name of the project to be published",
      "required": true
    }
  ]
}
